[project]
name = "05-deployment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Omer Yaslitas", email = "omer.yaslitas@metu.edu.tr" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "scikit-learn>=1.9.0",
    "uvicorn>=0.52.3",
]

[project.scripts]
05-deployment = "05_deployment:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "requests>=2.34.2",
]
